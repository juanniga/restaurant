export class GlobalConstans{
    //mensage
    public static genericError:string = "Algo paso. Por favor intente despues";

    //Regex
    public static nameRegex:string = "[a-zA-Z0-9 ]*";
    public static emailRegex:string = "[A-Za-z0-9._%-]+@[A-Za-z0-9._%-]+\\.[a-z]{2,3}";
    public static contactNumberRegex:string = "^[0-9]{8,8}$";
    public static number:string = "^[0-9]{1,2}$";
    //Variable
    public static error:string = "error";
      //message
 

      public static authroized:string = "You are not authorized person to access this page";
  
      public static productExistError:string="Product already exist";
      public static productAdded:string="Product Added Successfully";
      //regex
  
  
      //variable

}